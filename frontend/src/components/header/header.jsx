import { Layout } from 'antd';

const { Header } = Layout;

function MainHeader() {
  return (
    <Header style={{display: 'flex', alignItems: 'center'}}>
      
    </Header>
  )
}

export default MainHeader;